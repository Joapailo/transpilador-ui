{
  "name": "expo-api-server",
  "version": "1.0.0",
  "description": "Servidor para criar projetos Expo via API",
  "main": "index.js",
  "scripts": {
    "start": "npm install -g expo-cli && node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "shelljs": "^0.8.5"
  },
  "engines": {
    "node": "18.x"
  }
}
